sender:
  - collect:
      pattern:
        - ./data/*.json
      maxFilesBatch: 5
      delay: 1
    workers: 1
    topic: collector.files

  - collect:
      pattern:
        - ./data/*.txt
      maxFilesBatch: 1
      delay: 3
    workers: 2
    topic: collector.files
