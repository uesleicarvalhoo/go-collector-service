run:
  deadline: 5m
  tests: false

linters:
  enable-all: true
  disable:
  # Deprecated
  - maligned
  - scopelint
  - golint
  - interfacer
  # Not avaliable from go v1.18
  - bodyclose
  - contextcheck
  - gosimple
  - nilerr
  - noctx
  - rowserrcheck
  - sqlclosecheck
  - staticcheck
  - structcheck
  - stylecheck
  - tparallel
  - unparam
  - wastedassign
  - unused
  # Only Disabled
  - ireturn
  - exhaustivestruct
  - wrapcheck

linters-settings:
  lll:
    line-length: 120

issues:
  exclude-rules:
    - text: "G402: TLS MinVersion too low."
      linters:
        - gosec
    - text: "mnd: Magic number: (\\d+)"
      linters:
        - gomnd
